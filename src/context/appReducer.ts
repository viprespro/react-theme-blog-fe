const ApplicationReducer = (state: any, action: any) => {
  let newState: any
  switch (action.type) {
    default:
      newState = state
      break
  }
  return newState
}

export default ApplicationReducer
